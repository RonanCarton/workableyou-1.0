<%= search_form_for@q do |f| %>
    <div class="field">
      <%= f.label :event_location_cont, "Event Location" %>
      <%= f.text_field :event_location_cont %>
    </div>
    <div class="field">
      <%= f.label :event_date_cont, "Month" %>
      <%= f.text_field :event_date_cont %>
    </div>

    <div class="actions"><%= f.submit "Search" %></div>
<%end%>